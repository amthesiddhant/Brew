Brew 1.9.2 is a small polish update.

What's new in this version:

• The version shown at the bottom of the window now always matches the version you're running — it updates automatically with every update.

• The window now scrolls and adjusts itself, so nothing gets cut off — even when the update panel expands on smaller screens.

Enjoy your coffee. ☕
